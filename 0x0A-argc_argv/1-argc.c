#include <stdio.h>

/*
 * main - Entry point
 * @argc: arguments count 
 * @argv: arguments array
 * Return: returns 0 Always (success)
 */

int main(int argc, char* argv[])
{
	printf("%d\n", argc);
	return (0);
}
